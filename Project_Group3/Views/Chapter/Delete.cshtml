@model Project_Group3.Models.ModelsView
@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Login.cshtml";
}

<link rel="stylesheet" href="~/css/site.css" />
<link rel="stylesheet" href="~/fonts/boxicons.css" />

<div class="container-fluid mt-2 mb-3 p-0">
    <div class="card mb-4 bg-theme">
        <form asp-action="Delete" class="ml-5 mt-3 mb-3" enctype="multipart/form-data">

            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <input type="hidden" asp-for="@Model.Chapter.ChapterId">
                            <input type="hidden" asp-for="@Model.Chapter.CourseId" >
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.Chapter.ChapterName" class="text-theme font-weight-bold"></label>
                            <input asp-for="@Model.Chapter.ChapterName" class="form-control" readonly /> <!-- Thêm readonly vào đây -->
                            <span asp-validation-for="@Model.Chapter.ChapterName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.Chapter.Index" class="text-theme font-weight-bold"></label>
                            <input asp-for="@Model.Chapter.Index" class="form-control" readonly /> <!-- Thêm readonly vào đây -->
                            <span asp-validation-for="@Model.Chapter.Description" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.Chapter.Description" class="text-theme font-weight-bold"></label>
                            <input asp-for="@Model.Chapter.Description" class="form-control" readonly /> <!-- Thêm readonly vào đây -->
                            <span asp-validation-for="@Model.Chapter.Description" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="@Model.Chapter.TotalTime" class="text-theme font-weight-bold"></label>
                            <input asp-for="@Model.Chapter.TotalTime" class="form-control" readonly /> <!-- Thêm readonly vào đây -->
                            <span asp-validation-for="@Model.Chapter.TotalTime" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>

            <button type="submit" class="btn btn-success mr-3">Delete</button>
            <a href="@Url.Action("Index", "Chapter", new {courseId = @Model.Chapter.CourseId})" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</div>

@{
    var errorMessage = ViewBag.Message as string;
}

@if (!string.IsNullOrEmpty(errorMessage))
{
    <div class="alert alert-danger">@errorMessage</div>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
